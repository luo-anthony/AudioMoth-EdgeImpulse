A Utility to Design Filters
---------------------------

On macOS and Linux use gcc to build the binary:

> gcc -o designFilters designFilters.c digitalFilter.c

On Windows use the 'x64_x86 Cross Tools Command Prompt' to build the 32-bit binary:

> cl designFilters.c digitalFilter.c /link /out:designFilters.exe

